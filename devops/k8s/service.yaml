# AutoPilotDev - Kubernetes Services
# Generated by AutoPilotDev AI

apiVersion: v1
kind: Service
metadata:
  name: autopilotdev-backend
  namespace: autopilotdev
  labels:
    app: autopilotdev
    component: backend
spec:
  selector:
    app: autopilotdev
    component: backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: autopilotdev-frontend
  namespace: autopilotdev
  labels:
    app: autopilotdev
    component: frontend
spec:
  selector:
    app: autopilotdev
    component: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: autopilotdev-mongodb
  namespace: autopilotdev
  labels:
    app: autopilotdev
    component: mongodb
spec:
  selector:
    app: autopilotdev
    component: mongodb
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: autopilotdev-redis
  namespace: autopilotdev
  labels:
    app: autopilotdev
    component: redis
spec:
  selector:
    app: autopilotdev
    component: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  type: ClusterIP
---
# Load Balancer for external access
apiVersion: v1
kind: Service
metadata:
  name: autopilotdev-loadbalancer
  namespace: autopilotdev
  labels:
    app: autopilotdev
    component: loadbalancer
spec:
  selector:
    app: autopilotdev
    component: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  type: LoadBalancer