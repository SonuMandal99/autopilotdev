# AutoPilotDev - Kubernetes Ingress
# Generated by AutoPilotDev AI

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: autopilotdev-ingress
  namespace: autopilotdev
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - autopilotdev.example.com
    secretName: autopilotdev-tls
  rules:
  - host: autopilotdev.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: autopilotdev-frontend
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: autopilotdev-backend
            port:
              number: 5000
      - path: /health
        pathType: Exact
        backend:
          service:
            name: autopilotdev-backend
            port:
              number: 5000
      - path: /metrics
        pathType: Exact
        backend:
          service:
            name: autopilotdev-backend
            port:
              number: 5000
      - path: /docs
        pathType: Prefix
        backend:
          service:
            name: autopilotdev-backend
            port:
              number: 5000